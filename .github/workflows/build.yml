name: test build
on: [ push ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build project # Nothing to do for now
        run: |
            sudo apt install mkisofs
            echo Done
